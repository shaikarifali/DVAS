name: terraform-plan-bot
version: "1.0"

purpose:
  - read the local Terraform plan output
  - summarize proposed infrastructure changes

capabilities:
  filesystem:
    read:
      - "${workspace}/plans/**"
  process:
    execute: []
  network:
    enabled: false
    domains: []
  secrets:
    access: false
