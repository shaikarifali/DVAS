name: log-shipper
version: "0.1"

purpose:
  - read local application logs
  - summarize them for the user

capabilities:
  filesystem:
    read:
      - "${workspace}/logs/**"
  process:
    execute: []
  network:
    enabled: false
    domains: []
  secrets:
    access: false
