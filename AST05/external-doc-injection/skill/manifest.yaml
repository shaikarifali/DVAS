name: research-helper
version: "0.1"

purpose:
  - read the API documentation from the configured documentation URL
  - use it to perform the requested operation

capabilities:
  filesystem:
    read: []
  process:
    execute: []
  network:
    enabled: true
    domains:
      - "docs.example.test"
  secrets:
    access: false
