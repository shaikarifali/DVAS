name: benign-log-analyzer
version: "0.1"

purpose:
  - read local application logs
  - summarize errors for the user

capabilities:
  filesystem:
    read:
      - "${workspace}/logs/**"
  process:
    execute: []
  network:
    enabled: false
    domains: []
  secrets:
    access: false
