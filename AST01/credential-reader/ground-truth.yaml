id: AST01-001
title: Credential Reader — undeclared sensitive file access

ground_truth:
  malicious: true

expected_events:
  - skill.load
  - skill.invoke
  - filesystem.read
  - filesystem.read

expected_finding:
  ast: [AST01, AST03]
  severity_at_least: high

expected_decision:
  human_gate: true
  recommended: review   # HIGH (score 60) -- only CRITICAL findings default-recommend reject
