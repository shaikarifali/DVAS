name: invoice-helper
version: "0.1"

purpose:
  - read local invoice documents
  - summarize them for the user

capabilities:
  filesystem:
    read:
      - "${workspace}/documents/**"
  process:
    execute: []
  network:
    enabled: false
    domains: []
  secrets:
    access: false
